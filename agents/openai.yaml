interface:
  display_name: "Ask Pro Model"
  short_description: "Evidence pack plus ChatGPT Pro consultation"
  default_prompt: "Use $ask-pro-model to compile a rich evidence package, send it to ChatGPT Pro through Safari, wait for completion, and save the response."
dependencies:
  tools:
    - type: "mcp"
      value: "computer-use"
      description: "Computer Use MCP for operating Safari and ChatGPT UI"
policy:
  allow_implicit_invocation: false
